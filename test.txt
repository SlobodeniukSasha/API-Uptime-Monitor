Models:

User_model:
    -id
    -email
    -hashed_password
    -full_name
    -created_at

Monitor_model:
    -id
    -url
    -name
    -expected_status_code
    -check_interval
    -is_active
    -created_at

History_model:
    -id
    -monitor_id
    -monitor_name
    -status # 'healthy', 'down', 'degraded' !!!!!
    -status_code # 200, 300, 400, 500
    -latency
    -error_message # message | None
    -checked_at

Monitor_Problem_detail_model:
    -id
    -monitor_id
    -monitor_name
    -status # 'healthy', 'down', 'degraded' !!!!!
    -status_code # 200, 300, 400, 500
    -latency
    -error_message # message | None
    -detail_with_DuckDuckGo # Body of result request
    -OpenAI_schema # recommendations of problem-solving
    -checked_at


Endpoints:

-Auth:
    - register
    - login
    - refresh
    - logout
-Monitor:
    -get_all
    -get_one
    -create
    -update
    -delete
-History:
    /monitors/{monitor_id}/history/
    /monitors/{monitor_id}/problem_history/
    /monitors/{monitor_id}/problem_history/{problem_monitor_id}



-Analysis:
    -Detail_Amount_of_Recent_Errors
